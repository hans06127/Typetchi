# Typetchi v0.8.1 SVG Renderer Hotfix

- Default renderer: `svg`.
- Runtime extension renderer source: `scripts/content-dist.js`, copied to `dist/content/index.js` by `npm run build`.
- React/source renderer source: `src/components/PetCharacter/PetCharacter.tsx`.
- The old CSS round pet is not used as the default renderer.
- Dev-only tools are hidden by default and toggled with `Alt + Shift + T`; use `關閉 Dev` to disable them.
